# 운영 워크플로우 상세

> **이것은 바이브 코딩이 아니다.** AI 출력을 읽지도 않고 수락하는 게 아니다.
> 모든 계획은 검토되고, 모든 결과물은 검증되며, 모든 결정 뒤에는 사람이 있다.
> AI는 절차를 따르는 도구가 아니라 판단을 수행하는 협업자다. 다만 모든 최종 의사결정과 품질 책임은 작업지시자에게 있다.

거시적 워터폴(계획→승인→구현→검증) + 미시적 애자일(단계별 반복)을 AI가 동시에 수행하게 한다.

## 핵심 원칙

| 구분 | 바이브 코딩 | 이 프로젝트 |
|------|------------|-------------|
| **사람의 역할** | AI 출력 수락 | 지시, 검토, 결정 |
| **계획** | 없음 | 계획서 작성 → 승인 → 실행 |
| **품질 관문** | 동작하길 바람 | 단계별 승인 + 테스트 통과 |
| **디버깅** | AI에게 AI 버그 수정 요청 | 사람이 진단, AI가 구현 |
| **문서** | 없음 | mydocs/ 에 전 과정 기록 |

### AI의 자율 판단 원칙

AI에게 구현 절차를 모두 지시하지 않는다.
필요한 경우 테스트 범위, 구현 방식, 적절한 모델 또는 서브에이전트 선택은 AI가 스스로 판단한다.
단, 설계 방향, 요구사항 변경, 품질 승인과 같은 최종 판단은 항상 작업지시자가 수행한다.

AI는 구현을 시작하기 전에 아래 순서대로 구현 필요성을 검토한다.

1. 구현하지 않고 해결 가능한가
2. 기존 코드·설정·패턴을 재사용할 수 있는가
3. 표준 라이브러리로 해결 가능한가
4. 플랫폼 기본 기능으로 해결 가능한가
5. 이미 사용 중인 의존성으로 해결 가능한가
6. 설정 변경·삭제·최소 변경으로 해결 가능한가
7. 위 방법으로 해결할 수 없는 경우에만 최소 코드를 작성한다.

### 디버깅 원칙

버그 보고는 증상으로 간주한다.

수정 전에 재현, 실행 흐름, 관련 호출 경로를 확인하고 근본원인을 찾는다.
동일한 원인을 공유하는 공통 지점이 있다면 해당 위치를 수정하며, 호출자마다 동일한 수정은 가능한 피한다.

## 병렬 작업 원칙

하나의 타스크는 여러 **작업 단위(Work Item)** 로 분리하여 병렬 수행할 수 있다.

각 작업 단위는 하나 이상의 AI 또는 서브에이전트가 담당할 수 있으며, 담당 AI는 자신의 작업 범위에 대해서만 계획, 구현, 검증 및 문서를 작성한다.

작업지시자는 개별 작업 단위의 모든 보고서를 검토하는 것이 아니라, 단계 승인요약서를 기준으로 승인 여부를 판단한다.

필요한 경우에만 단계별 완료보고서를 추가로 확인한다.


## 타스크 진행 절차

모든 정식 타스크는 아래 절차를 따른다. 단계를 건너뛰지 않는다.

1. GitHub Issue 등록 (또는 `## TODO / 백로그` 항목 참조)
2. 오늘할일(`mydocs/orders/yyyymmdd.md`) 기록
3. 타스크 브랜치 생성 (`local/task{번호}`)
4. 관련 코드·문서·설정과 실제 실행 흐름 조사
5. **수행계획서** 작성 -> **[작업지시자 승인]**
6. **구현계획서** 작성(최소 변경 원칙 적용) -> **[작업지시자 승인]**
7. 작업 단위를 정의하고 필요한 경우 여러 AI 또는 서브에이전트가 병렬 수행
8. 각 작업 담당자는 자신의 작업 결과를 **단계별 완료보고서**에 기록한다.
9. 단계 책임자(또는 메인 에이전트)는 단계별 완료보고서를 검토하여 현재 단계의 변경사항, 결정사항, 위험요소, 작업지시자가 확인해야 할 사항만 정리한 **단계 승인요약서(Stage Summary)** 를 작성한다.
10. 작업지시자는 단계 승인요약서를 기준으로 승인 여부를 결정한다. 필요한 경우에만 개별 단계별 완료보고서를 확인한다.
11. 모든 단계가 완료되면 **최종 결과보고서**를 작성하여 **[작업지시자 승인]** 을 받는다.
12. 오늘할일 상태 갱신
13. 반복 가능한 교훈만 tech/spec/troubleshootings에 승격

각 **[승인]** 지점이 품질 게이트다. 방향성 오류를 코드가 쌓이기 전에 잡는다.
각 품질 게이트에서는 요구사항 충족 여부뿐 아니라 다음 사항도 함께 확인한다.

- 기존 구현을 재사용할 수 없는지 검토되었는가
- 변경 범위가 필요한 최소 수준인가
- 요청되지 않은 기능이나 추상화가 추가되지 않았는가
- 근본원인 위치에 수정이 이루어졌는가
- 변경으로 인한 회귀 가능성이 확인되었는가

**결과보고서**는 변경에서 일어난 모든 일에 대한 HTML 보고서를 별도로 만든다. 사람이 읽고 이해할 수 있도록 context, 직관, 수행된 작업 등을 포함하고, 맨 아래에는 사람이 반드시 통과해야 하는 퀴즈를 넣는다.

## 문서 체계 (mydocs/)

```
mydocs/
├── orders/           # 오늘 할일 (yyyymmdd.md) — 새 세션에서 "지금 뭘 해야 하지?" 답
├── plans/            # 수행계획서 + 구현계획서 — "어떻게 할 것인가"
│   └── archives/     # 완료된 계획서 보관
├── working/          # 작업 단위별 완료보고서 + 단계 승인요약서 — "어디까지 했는가"
├── report/           # 최종 결과보고서 — "결과가 무엇인가"
├── feedback/         # 코드 리뷰 피드백 — "무엇이 틀렸는가" (AI가 스스로 만들 수 없는 문서)
├── tech/             # 기술 사항 정리 — "무엇을 발견했는가" (세션 간 지식 영구화)
├── spec/             # 설계 사항 정리 — "무엇을 기반으로 하는가" (세션 간 지식 영구화)
└── troubleshootings/ # 트러블슈팅 — "이 함정에 다시 빠지지 마라"
```

**왜 문서가 필요한가**: AI는 세션이 끊기면 기억이 사라진다. 문서가 있으면 새 세션에서도 컨텍스트 전달 없이 `orders/` → `working/` → `plans/` 순으로 읽어 즉시 작업 재개 가능.

## 문서 파일명 규칙

| 문서 | 위치 | 파일명 | 예시 |
|------|------|--------|------|
| 오늘 할일 | `orders/` | `yyyymmdd.md` | `20260525.md` |
| 수행 계획서 | `plans/` | `task_{번호}.md` | `task_1.md` |
| 구현 계획서 | `plans/` | `task_{번호}_impl.md` | `task_1_impl.md` |
| 단계별 완료보고서 | `working/` | `task_{번호}_{작업단위}_stage{N}.md` | `task_1_backend_stage1.md` |
| 단계 승인요약서 | `working/` | `task_{번호}_{작업단위}_stage{N}_summary.md` | `task_1_backend_stage1_summary.md` |
| 최종 보고서 | `report/` | `task_{번호}_report.md` | `task_1_report.md` |
| 피드백 | `feedback/` | `task_{번호}_feedback.md` | `task_1_feedback.md` |
| 기술 정리 | `tech/` | 주제별 자유 명명 | `spring_security_block.md` |
| 설계 정리 | `spec/` | 주제별 자유 명명 | `spring_spec.md` |
| 트러블슈팅 | `troubleshootings/` | 주제별 자유 명명 | `ims_keepalive_bug.md` |


### 작업단위 이름 규칙

작업단위는 구현 기술이 아니라 사람이 이해할 수 있는 업무 단위로 작성한다.

좋은 예

- backend
- frontend
- infra
- docs
- test

지양

- worker1
- api2
- temp
- fix
- misc
